/**
 * Public surface of the speech module. Import from `$lib/tts`; the internals
 * (`sherpa.ts`, `sherpa.worker.ts`, `webspeech.ts`, `cache.ts`) are
 * implementation detail.
 */

export * from './tts';
