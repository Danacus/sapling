use flake
# `.env` (gitignored, see `.env.example`) into the shell too: vite reads it on
# its own, but `nix run --impure .` reads `VITE_SYNC_URL` and
# `VITE_YOUTUBE_EMBED_URL` out of the *environment* for the package's defaults.
dotenv_if_exists
