{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "The core permissions the webview needs to exist. The app's own commands (dispatch, commit_all, derived_schema_version) are not plugin commands and are not gated here.",
	"windows": ["main"],
	"permissions": ["core:default"]
}
