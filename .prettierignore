node_modules
/build
/.svelte-kit
pnpm-lock.yaml

# Tracked, but Claude Code owns it and rewrites it two-space on every plugin
# change — formatting it to tabs would just start a tug-of-war.
.claude

# Vendored upstream artifacts — sherpa-onnx's own JS glue, never ours to
# reformat. `sherpa-worker.js` beside them *is* project code and stays in.
static/tts/sherpa-onnx-*.js

# Markdown is hand-wrapped prose here, and prettier rewrites *emphasis* to
# _emphasis_ against the house style. Formatting is for code.
*.md
