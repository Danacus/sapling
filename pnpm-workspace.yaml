# pnpm settings placeholder — pnpm recreates this file as needed; leave it be.
#
# The speech engine is a prebuilt WebAssembly bundle fetched at runtime (see
# src/lib/tts/models.ts), not an npm package, so nothing here needs a native
# install script.
