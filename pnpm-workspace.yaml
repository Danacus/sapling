# pnpm settings.
#
# Nothing to configure right now: the speech engine is a prebuilt WebAssembly
# bundle fetched at runtime (see src/lib/tts/models.ts), not an npm package, so
# no dependency here has a native install script that needs allowing or
# blocking. Kept as a placeholder because pnpm recreates it on demand.
